import ServicesSlider from '../components/ServicesSlider'

export default function Home() {
  return (
        <main className="snap-x snap-mandatory overflow-x-auto h-screen w-screen">
      <ServicesSlider />
    </main>
  )
}
